import {createClient} from "@supabase/supabase-js";
function db(){return createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);}
function auth(req){return req.headers.get("x-admin-key")===process.env.ADMIN_PASSWORD;}
export async function GET(req){if(!auth(req))return Response.json({error:"Unauthorized"},{status:401});const {data,error}=await db().from("products").select("*").order("created_at",{ascending:false});if(error)return Response.json({error:error.message},{status:400});return Response.json({products:data||[]});}
export async function POST(req){if(!auth(req))return Response.json({error:"Unauthorized"},{status:401});const b=await req.json();const p={name:b.name,price:Number(b.price),tag:b.tag||"",conc:b.conc||"",longevity:Number(b.longevity)||1,sillage:Number(b.sillage)||1,top:b.top||"",heart:b.heart||"",base:b.base||"",desc:b.desc||"",stock:Number(b.stock)||0,image_url:b.image_url||""};const {data,error}=await db().from("products").insert([p]).select().single();if(error)return Response.json({error:error.message},{status:400});return Response.json({product:data});}
export async function PUT(req){if(!auth(req))return Response.json({error:"Unauthorized"},{status:401});const b=await req.json();const p={name:b.name,price:Number(b.price),tag:b.tag||"",conc:b.conc||"",longevity:Number(b.longevity)||1,sillage:Number(b.sillage)||1,top:b.top||"",heart:b.heart||"",base:b.base||"",desc:b.desc||"",stock:Number(b.stock)||0,image_url:b.image_url||""};const {data,error}=await db().from("products").update(p).eq("id",b.id).select().single();if(error)return Response.json({error:error.message},{status:400});return Response.json({product:data});}
export async function DELETE(req){if(!auth(req))return Response.json({error:"Unauthorized"},{status:401});const {id}=await req.json();const {error}=await db().from("products").delete().eq("id",id);if(error)return Response.json({error:error.message},{status:400});return Response.json({ok:true});}
